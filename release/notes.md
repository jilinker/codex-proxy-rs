# v3.13.1+4

## 体验优化

- 分组 Key 授权改用后端分组筛选 搜索和分页 仅返回当前页数据 总数按相同条件计算
- 授权弹窗复用系统分页组件 支持调整每页条数 翻页和搜索时保留跨页勾选状态
- 授权回显仅返回仍绑定当前分组的已授权 Key ID

## 接口调整

- 管理端 Key 列表增加 `groupId` 和 `page` 查询参数 保留原有游标分页 页码与游标不能同时使用
